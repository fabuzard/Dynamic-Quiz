/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",          // Include index.html from the root
    "./src/**/*.{html,js}",  // Include all HTML and JS files inside the src folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
